// package Tannu.*;
import Tannu.paras.friends.*;
public class Shapesss{  
    public static void main(String[] args) {

        //Creating the object of Cylinder class and using all its methods
        Tannu.paras.friends.Cylinder cy = new Tannu.paras.friends.Cylinder();
        cy.getSet();
        cy.area();
        
        //Creating the object of Rectangle class and using its methods
        Tannu.paras.friends.Rectangle rec = new Tannu.paras.friends.Rectangle();
        rec.getSet();
        rec.area();

        //Creating the object of Sphere class and using all its methods
        Tannu.paras.friends.Sphere sp = new Tannu.paras.friends.Sphere();
        sp.getSet();
        sp.area();

        //Creating the object of Square class and using all its methods
        Tannu.paras.friends.Square sq = new Tannu.paras.friends.Square();
        sq.getSet();
        sq.getClass();
        sq.area();

        // cr.close();
        // cy.close();
        // rec.close();
        // sp.close();
        // sq.close();

    }
}


